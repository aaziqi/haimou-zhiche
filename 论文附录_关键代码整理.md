# 论文附录：关键部分代码整理

本文系统由“本地交互式 Web 前端 + Python 推理与评测服务 + 水下图像增强模型 + 下游检测联动模块”组成。附录部分建议优先展示能够体现系统核心流程、算法调用与工程实现的关键代码。以下内容已按“功能说明 + 代码片段 + 源文件位置”整理，可直接用于论文附录。

## 一、后端服务入口与接口分发

该部分代码用于构建本地交互式演示服务，向前端提供系统初始化信息、图像增强、检测对比、综合对比和归档查询等接口，是整个系统前后端联动的核心入口。

源文件：

- [interactive_demo_server.py](file:///d:/VScode/Graduation%20project/pytorch-CycleGAN-and-pix2pix-master/pytorch-CycleGAN-and-pix2pix-master/scripts/interactive_demo_server.py#L1012-L1269)

```python
def bootstrap_payload() -> dict:
    return {
        "ok": True,
        "models": build_model_presets(),
        "traditional_methods": TRADITIONAL_METHODS,
        "benchmarks": build_benchmark_panels(),
        "presentation_pack": PRESENTATION_PACK,
        "archives": load_archive_entries()[:12],
        "detectors": DETECTOR_PRESETS,
    }


class DemoRequestHandler(SimpleHTTPRequestHandler):
    def __init__(self, *args, directory: str | None = None, gpu_ids: str = "-1", **kwargs):
        self.gpu_ids = gpu_ids
        super().__init__(*args, directory=str(WEB_ROOT), **kwargs)

    def do_GET(self):
        parsed = urlparse(self.path)
        if parsed.path == "/api/bootstrap":
            json_response(self, bootstrap_payload())
            return
        if parsed.path == "/api/archive":
            json_response(self, {"ok": True, "archives": load_archive_entries()})
            return
        if parsed.path == "/api/health":
            json_response(self, {"ok": True})
            return
        if parsed.path == "/":
            self.path = "/index.html"
        super().do_GET()

    def do_POST(self):
        parsed = urlparse(self.path)
        try:
            if parsed.path == "/api/enhance":
                payload = read_json_request(self)
                filename = payload.get("filename", "upload.png")
                image_data = payload.get("image", "")
                model_key = payload.get("model_key", "improved_mpcgan")
                job_id = f"enhance_{uuid.uuid4().hex[:10]}"
                upload_root = RUNTIME_ROOT / "enhance_jobs" / job_id / "upload"
                input_path = decode_image_payload(filename, image_data, upload_root)
                preset = pick_model(model_key)
                result = run_single_model(
                    RUNTIME_ROOT / "enhance_jobs" / job_id / "result",
                    input_path,
                    preset,
                    self.gpu_ids,
                )
                add_archive_entry(build_archive_entry_for_enhance(job_id, result))
                json_response(self, {"ok": True, "job_id": job_id, "result": result})
                return

            if parsed.path == "/api/detect_compare":
                payload = read_json_request(self)
                raw_image_url = payload.get("raw_image_url", "")
                enhanced_image_url = payload.get("enhanced_image_url", "")
                detector_key = payload.get("detector_key", "default_detector")
                ...
                raw_dets = run_detector_on_image(...)
                enhanced_dets = run_detector_on_image(...)
                report_url = build_detect_compare_report(...)
                add_archive_entry(build_archive_entry_for_detect_compare(...))
                json_response(self, self, {"ok": True, "report_url": report_url})
                return

            if parsed.path == "/api/compare":
                payload = read_json_request(self)
                filename = payload.get("filename", "upload.png")
                image_data = payload.get("image", "")
                traditional_key = payload.get("traditional_key", "grayworld_clahe")
                improved_key = payload.get("improved_key", "improved_mpcgan")
                input_path = decode_image_payload(filename, image_data, RUNTIME_ROOT / "incoming")
                result = build_compare_job(input_path, traditional_key, improved_key, self.gpu_ids)
                add_archive_entry(build_archive_entry_for_compare(result["job_id"], result))
                json_response(self, {"ok": True, "result": result})
                return
        except Exception as exc:
            error_response(self, str(exc), HTTPStatus.INTERNAL_SERVER_ERROR)
```

可在正文中配套说明：

- `/api/bootstrap` 负责向前端下发模型、传统方法、评测结果、归档案例和检测器配置。
- `/api/enhance` 负责单模型实时增强。
- `/api/compare` 负责传统方法、基线模型和改进模型的综合对比。
- `/api/detect_compare` 负责原图与增强图的下游检测联动验证。

## 二、增强模型推理命令构造

该部分代码体现了系统如何自动读取训练配置、识别权重命名方式，并构造 CycleGAN 测试命令完成单图或批量推理，是模型调用环节的关键实现。

源文件：

- [underwater_competition_demo.py](file:///d:/VScode/Graduation%20project/pytorch-CycleGAN-and-pix2pix-master/pytorch-CycleGAN-and-pix2pix-master/scripts/underwater_competition_demo.py#L174-L236)

```python
def build_test_command(
    input_dir: Path,
    output_dir: Path,
    checkpoint_name: str,
    epoch: str,
    direction: str,
    num_test: int,
) -> tuple[list[str], Path]:
    checkpoint_dir = CHECKPOINTS_DIR / checkpoint_name
    if not checkpoint_dir.exists():
        raise FileNotFoundError(f"未找到 checkpoint 目录: {checkpoint_dir}")

    suffix = detect_model_suffix(checkpoint_dir, epoch, direction)
    if suffix is None:
        raise FileNotFoundError(f"未找到可用生成器权重: {checkpoint_dir}")

    options = parse_train_opt(checkpoint_dir)
    command = [
        sys.executable,
        str(TEST_PY),
        "--dataroot", str(input_dir),
        "--name", checkpoint_name,
        "--model", "test",
        "--dataset_mode", "single",
        "--results_dir", str(output_dir / "inference_raw"),
        "--epoch", epoch,
        "--num_test", str(num_test),
    ]

    passthrough_keys = [
        "netG", "norm", "ngf", "input_nc", "output_nc",
        "load_size", "crop_size", "preprocess",
    ]
    for key in passthrough_keys:
        value = options.get(key)
        if value:
            command.extend([f"--{key}", value])

    if options.get("no_dropout", "").lower() == "true":
        command.append("--no_dropout")
    if suffix:
        command.extend(["--model_suffix", suffix])

    web_dir = output_dir / "inference_raw" / checkpoint_name / f"test_{epoch}"
    return command, web_dir


def run_inference(command: list[str], gpu_ids: str) -> None:
    env = dict(**__import__("os").environ)
    if gpu_ids.strip() == "-1":
        env["CUDA_VISIBLE_DEVICES"] = ""
        env["CYCLEGAN_DEVICE"] = "cpu"
    completed = subprocess.run(command, cwd=REPO_ROOT, capture_output=True, text=True, env=env)
    if completed.returncode != 0:
        raise RuntimeError(f"{completed.stdout}\n{completed.stderr}".strip())
```

可在正文中配套说明：

- 程序通过读取 `train_opt.txt` 自动继承训练时的关键参数，避免推理配置与训练配置不一致。
- 程序兼容 `_A`、`_B` 和通用 `G` 三种权重命名方式，提高模型接入的灵活性。
- 该设计使系统能够快速切换不同增强模型，实现工程化复用。

## 三、图像质量指标计算与记录

该部分代码体现了系统如何对输入图像、增强结果和参考图像进行统一评测，并生成适合报告展示的数据记录，是实验分析与结果可验证性的关键支撑。

源文件：

- [underwater_competition_demo.py](file:///d:/VScode/Graduation%20project/pytorch-CycleGAN-and-pix2pix-master/pytorch-CycleGAN-and-pix2pix-master/scripts/underwater_competition_demo.py#L246-L341)

```python
@dataclass
class ImageRecord:
    key: str
    input_file: str
    output_file: str
    reference_file: str
    input_uciqe: float | None
    output_uciqe: float | None
    delta_uciqe: float | None
    input_uiqm: float | None
    output_uiqm: float | None
    delta_uiqm: float | None
    input_psnr: float | None
    output_psnr: float | None
    delta_psnr: float | None
    input_ssim: float | None
    output_ssim: float | None
    delta_ssim: float | None


def compute_record(
    metrics_module,
    key: str,
    input_path: Path,
    output_path: Path,
    reference_path: Path | None,
    assets_dir: Path,
) -> ImageRecord:
    input_asset = relative_copy(input_path, assets_dir / "input", input_path.name)
    output_asset = relative_copy(output_path, assets_dir / "output", output_path.name)
    reference_asset = ""
    input_uciqe = output_uciqe = delta_uciqe = None
    input_uiqm = output_uiqm = delta_uiqm = None
    input_psnr = output_psnr = delta_psnr = None
    input_ssim = output_ssim = delta_ssim = None

    if metrics_module is not None:
        input_bgr = metrics_module.read_image(input_path)
        output_bgr = metrics_module.read_image(output_path)
        input_uciqe = metrics_module.compute_uciqe(input_bgr)
        output_uciqe = metrics_module.compute_uciqe(output_bgr)
        input_uiqm = metrics_module.compute_uiqm(input_bgr)
        output_uiqm = metrics_module.compute_uiqm(output_bgr)
        delta_uciqe = output_uciqe - input_uciqe
        delta_uiqm = output_uiqm - input_uiqm

        if reference_path is not None and reference_path.exists():
            reference_asset = relative_copy(reference_path, assets_dir / "reference", reference_path.name)
            reference_bgr = metrics_module.read_image(reference_path)
            aligned_input = resize_like(metrics_module.cv2, input_bgr, reference_bgr)
            aligned_output = resize_like(metrics_module.cv2, output_bgr, reference_bgr)
            input_psnr = metrics_module.compute_psnr(aligned_input, reference_bgr)
            output_psnr = metrics_module.compute_psnr(aligned_output, reference_bgr)
            input_ssim = metrics_module.compute_ssim(aligned_input, reference_bgr)
            output_ssim = metrics_module.compute_ssim(aligned_output, reference_bgr)
            delta_psnr = output_psnr - input_psnr
            delta_ssim = output_ssim - input_ssim

    return ImageRecord(
        key=key,
        input_file=f"assets/input/{input_asset}",
        output_file=f"assets/output/{output_asset}",
        reference_file=f"assets/reference/{reference_asset}" if reference_asset else "",
        input_uciqe=safe_round(input_uciqe),
        output_uciqe=safe_round(output_uciqe),
        delta_uciqe=safe_round(delta_uciqe),
        input_uiqm=safe_round(input_uiqm),
        output_uiqm=safe_round(output_uiqm),
        delta_uiqm=safe_round(delta_uiqm),
        input_psnr=safe_round(input_psnr),
        output_psnr=safe_round(output_psnr),
        delta_psnr=safe_round(delta_psnr),
        input_ssim=safe_round(input_ssim),
        output_ssim=safe_round(output_ssim),
        delta_ssim=safe_round(delta_ssim),
    )
```

可在正文中配套说明：

- 无参考指标采用 `UCIQE` 和 `UIQM`，用于评价增强后图像的综合色彩与清晰度表现。
- 有参考指标采用 `PSNR` 和 `SSIM`，用于评估增强图像与参考真值之间的相似程度。
- 程序将输入、输出、参考图像及各项指标统一封装为 `ImageRecord`，便于后续生成 HTML、CSV 和 JSON 报告。

## 四、下游检测联动验证

该部分代码展示了系统如何自动加载 YOLO 检测器、执行检测推理并输出可视化图和 JSON 结果，用于验证增强结果对下游视觉任务的促进作用。

源文件：

- [interactive_demo_server.py](file:///d:/VScode/Graduation%20project/pytorch-CycleGAN-and-pix2pix-master/pytorch-CycleGAN-and-pix2pix-master/scripts/interactive_demo_server.py#L129-L268)

```python
def build_detector_presets() -> list[dict]:
    project_root = resolve_project_root()
    weights_env = os.environ.get("DEMO_DETECT_WEIGHTS", "").strip()
    data_env = os.environ.get("DEMO_DETECT_DATA_YAML", "").strip()

    weights_candidates = [
        Path(weights_env) if weights_env else None,
        project_root / "results" / "yolo_urpc_train" / "yolov8n_urpc_final" / "weights" / "best.pt",
        project_root / "yolov8n.pt",
        project_root / "yolo11n.pt",
        REPO_ROOT / "yolo11n.pt",
    ]
    data_candidates = [
        Path(data_env) if data_env else None,
        project_root / "datasets" / "URPC_optical" / "yolo_new" / "data.yaml",
        project_root / "datasets" / "URPC_optical" / "yolo" / "data.yaml",
    ]

    weights_path = next((p.resolve() for p in weights_candidates if isinstance(p, Path) and p.exists()), None)
    data_yaml = next((p.resolve() for p in data_candidates if isinstance(p, Path) and p.exists()), None)
    names = load_names_from_data_yaml(data_yaml) if data_yaml else []

    presets: list[dict] = []
    if weights_path is not None:
        presets.append(
            {
                "key": "default_detector",
                "label": "下游检测器 (YOLO)",
                "description": "在增强结果上运行下游检测器，输出可视化图与检测结果 JSON。",
                "weights": str(weights_path),
                "data_yaml": str(data_yaml) if data_yaml else "",
                "names": names,
                "default_conf": 0.25,
                "default_imgsz": 640,
            }
        )
    return presets


def get_yolo_model(weights_path: Path):
    key = str(weights_path.resolve())
    with _YOLO_MODEL_LOCK:
        cached = _YOLO_MODEL_CACHE.get(key)
        if cached is not None:
            return cached
        from ultralytics import YOLO
        model = YOLO(key)
        _YOLO_MODEL_CACHE[key] = model
        return model


def run_detector_on_image(
    *,
    input_path: Path,
    output_vis_path: Path,
    output_json_path: Path,
    detector: dict,
    conf: float,
    imgsz: int,
):
    weights_path = Path(detector["weights"]).resolve()
    model = get_yolo_model(weights_path)
    image = Image.open(input_path).convert("RGB")
    results = model.predict(image, conf=float(conf), imgsz=int(imgsz), verbose=False)
    dets: list[dict] = []
    ...
    for i in range(len(xyxy)):
        x1, y1, x2, y2 = [float(x) for x in xyxy[i].tolist()]
        cls_id = int(cls[i].item())
        score = float(confs[i].item())
        dets.append(
            {
                "cls_id": cls_id,
                "cls_name": names[cls_id] if 0 <= cls_id < len(names) else str(cls_id),
                "conf": round(score, 6),
                "x1": round(x1, 3),
                "y1": round(y1, 3),
                "x2": round(x2, 3),
                "y2": round(y2, 3),
            }
        )
```

可在正文中配套说明：

- 程序优先从环境变量和默认目录中自动发现检测权重和类别配置文件。
- 通过缓存机制避免重复加载 YOLO 模型，降低多次检测时的时间开销。
- 检测输出同时保存为可视化图片和结构化 JSON，方便答辩展示与结果追踪。

## 五、前端启动与动态/静态双模式切换

该部分代码体现了前端在系统启动时自动拉取后端配置，并在后端不可用时切换到静态展示模式，从而兼顾本地完整演示与线上展示部署两种使用场景。

源文件：

- [app.js](file:///d:/VScode/Graduation%20project/pytorch-CycleGAN-and-pix2pix-master/pytorch-CycleGAN-and-pix2pix-master/local_web_demo/app.js#L670-L718)

```javascript
async function bootstrap() {
    let payload;
    try {
        const response = await fetch("/api/bootstrap", { cache: "no-store" });
        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }
        payload = await response.json();
        state.runtimeMode = payload.runtime_mode || "dynamic";
    } catch (error) {
        const response = await fetch("./data/bootstrap.json", { cache: "no-store" });
        if (!response.ok) {
            throw new Error("无法读取展示数据，请检查 data/bootstrap.json 是否存在。");
        }
        payload = await response.json();
        state.runtimeMode = "static";
    }

    state.bootstrap = payload;
    renderOptions(dom.modelSelect, payload.models);
    renderOptions(
        dom.compareModelSelect,
        payload.models.filter((item) => item.key !== "classic_cyclegan")
    );
    renderOptions(dom.traditionalSelect, payload.traditional_methods);

    if (dom.detectorSelect && Array.isArray(payload.detectors) && payload.detectors.length) {
        renderOptions(dom.detectorSelect, payload.detectors);
        dom.detectorSelect.value = payload.detectors[0].key;
        resetDetectPanel();
    } else {
        if (dom.detectorSelect) {
            dom.detectorSelect.innerHTML = "";
        }
        resetDetectPanel("检测器未就绪，请先准备 YOLO 权重并重启服务。");
    }

    if (payload.models.some((item) => item.key === "improved_mpcgan")) {
        dom.modelSelect.value = "improved_mpcgan";
    }
    if (payload.traditional_methods.some((item) => item.key === "grayworld_clahe")) {
        dom.traditionalSelect.value = "grayworld_clahe";
    }

    renderBenchmarkHighlights(payload.benchmarks);
    renderBenchmarks(payload.benchmarks);
    renderPresentationPack(payload.presentation_pack);
    renderArchiveCards(dom.archivePreviewGrid, payload.archives || []);

    if (isStaticMode()) {
        disableInteractiveRuntime();
        hydrateStaticPreview();
    }
}
```

可在正文中配套说明：

- 当前端成功访问 `/api/bootstrap` 时，系统进入动态模式，开放增强、对比和检测功能。
- 当前端无法访问后端时，系统自动切换到静态模式，读取预生成的 `bootstrap.json` 数据用于作品展示。
- 这种双模式设计提高了系统在答辩现场、成果展示和线上部署场景下的稳定性。

## 六、附录选取建议

若论文附录篇幅有限，建议优先保留以下三部分：

1. 后端服务入口与接口分发
2. 增强模型推理命令构造
3. 图像质量指标计算与记录

若希望进一步体现作品的软件工程化能力，可补充以下两部分：

1. 下游检测联动验证
2. 前端启动与动态/静态双模式切换

## 七、可直接放入论文的说明文字

附录中给出的关键代码主要包括后端服务接口分发、增强模型推理命令构造、图像质量指标计算、下游目标检测联动验证以及前端动态加载与静态回退机制等部分。这些代码共同构成了本系统从“图像输入 - 模型增强 - 指标评测 - 检测验证 - 结果展示”的完整软件链路，能够较好体现本项目在算法实现、系统集成和工程落地方面的关键技术内容。

